# C++ 面试

## 高频考点

- 对象生命周期、RAII、复制/移动语义和异常安全。
- `unique_ptr`、`shared_ptr`、`weak_ptr` 的选择与代价。
- 虚函数、对象布局、模板、STL 和迭代器失效。
- 未定义行为、内存泄漏、越界和 use-after-free。
- 线程、锁、原子操作、内存序和 data race。
- 编译链接、CMake、sanitizer、调试器和 profiler。

## 必须能回答

怎样定位一个偶发崩溃或数据竞争？性能热点如何从 Profile 进一步分析到缓存局部性、拷贝或锁竞争？
