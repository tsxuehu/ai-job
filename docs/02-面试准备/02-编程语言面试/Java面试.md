# Java 面试

## 高频考点

- HashMap、ConcurrentHashMap、泛型和类加载。
- Java Memory Model、`volatile`、`synchronized`、Lock 和 CAS。
- 线程池参数、队列、拒绝策略和 CompletableFuture。
- JVM 内存区域、GC、线程转储和堆转储。
- Spring Bean 生命周期、AOP、自动配置和请求链路。
- 事务失效、传播行为、隔离级别和连接池。

## 必须能回答

如何为慢外部模型调用设置独立线程池和超时？服务出现高 CPU、Full GC 或线程阻塞时收集哪些证据？
