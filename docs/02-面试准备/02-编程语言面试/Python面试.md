# Python 面试

## 高频考点

- 对象模型、可变性、引用、浅拷贝与深拷贝。
- decorator、iterator、generator、context manager。
- GIL 限制什么、不限制什么。
- threading、multiprocessing、asyncio 的选择。
- `async def` 中调用阻塞函数的影响与隔离。
- 类型标注、虚拟环境、依赖锁定和测试。
- Python Web 服务的 worker 模型、性能和内存定位。

## 必须能回答

CPU 密集与 IO 密集任务分别如何并发？一个 async API 偶发卡住整个 worker，应怎样验证是否存在阻塞调用？
