# 计算机网络

## 需要学习

- TCP/IP、连接建立与关闭、可靠传输和拥塞控制。
- HTTP/1.1、HTTP/2、HTTP/3、HTTPS 和 TLS。
- DNS、代理、负载均衡、CDN 和服务发现。
- REST、RPC、WebSocket、SSE 和序列化。
- 连接池、Keep-Alive、超时、重试、退避和幂等。

## 验收

能讲清一个请求从客户端到服务、数据库再返回的完整路径，并能指出 DNS、代理、连接池、应用和下游各层的失败点。
